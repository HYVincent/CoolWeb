<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.vincent.lwx.dao.FamiliesMapping">

	<!-- 添加家人到家人列表 -->
    <insert id="addFamilyToFamilyList" parameterType="com.vincent.lwx.bean.Families">
    	insert into family_list(phone,familyPhone,remark,time) values(
    		#{phone},
    		#{familyPhone},
    		#{remark},
    		#{time}
    	)
    </insert>
    
    <!-- 删除某一个 -->
    <delete id="deleteFamilyOne" parameterType="Map">
    	delete from family_list where phone = #{phone} and familyPhone = #{familyPHone}
    </delete>
    
    <!-- 获取某一个信息 -->
    <select id="selectFamilyOne" parameterType="Map" resultType="com.vincent.lwx.bean.Families">
    	select * from family_list where phone = #{phone} and familyPhone = #{familyPhone}
    </select>
    
    <!-- 获取所有 -->
    <select id="selectFamilyAll" parameterType="String" resultType="Map">
    	select * from family_list where phone = #{phone}
    </select>


</mapper>